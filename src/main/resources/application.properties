# SERVER CONFIG
server.port=6572

# POSTGRES DB
spring.datasource.url=jdbc:mariadb://localhost:6572/favores_web
spring.datasource.username=root
spring.datasource.password=vkqhfptm
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# hibernate option

## 1. create : 매번 스키마를 새로 생성하고, 기존 데이터를 날린다.
spring.jpa.hibernate.ddl-auto=create
## 2. update : 새로운 컬럼추가가 가능하지만 삭제는 불가능, 데이터를 유지한다.
#spring.jpa.hibernate.ddl-auto=update
## 3. validate : 운영시에는 해당옵션으로 사용한다.
#spring.jpa.hibernate.ddl-auto=validate

## show-sql : hibernate가 자동으로 만들어주는 SQL문을 콘솔에 표시한다.
spring.jpa.show-sql=true
## spring.jpa.properties.hibernate.format_sql : SQL문 안에 들어가는 값도 표시한다.
spring.jpa.properties.hibernate.format_sql=true

# LogBack(Log4j)
## LOGGING LEVEL : 1) ERROR 2) WARN 3) INFO 4) DEBUG 5) TRACE
logging.level.root = info

# swagger enable
springfox.documentation.swagger.v2.enabled=true